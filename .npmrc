; .npmrc — project-scoped npm settings for AlgoLens

; ----- Registry -----
registry=https://registry.npmjs.org/

; If you publish to npm, set NPM_TOKEN in CI and uncomment:
; //registry.npmjs.org/:_authToken=${NPM_TOKEN}

; Example: GitHub Packages (per-scope). Replace "your-scope" and uncomment:
; @your-scope:registry=https://npm.pkg.github.com/
; //npm.pkg.github.com/:_authToken=${GITHUB_PACKAGES_TOKEN}

; ----- Installs & lockfiles -----
package-lock=true
save-exact=true
legacy-peer-deps=false
strict-peer-deps=false
prefer-dedupe=true
engine-strict=true

; ----- Output & UX -----
progress=false
loglevel=warn
color=true
update-notifier=false
fund=false

; ----- Security/Audit -----
; Enable locally if you want: audit=true
audit=false

; ----- Caching -----
cache=.npm-cache
prefer-offline=false

; ----- Scripts (cross-platform) -----
scripts-prepend-node-path=true

; ----- Publish defaults -----
access=public
